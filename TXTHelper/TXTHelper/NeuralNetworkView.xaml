<UserControl x:Class="TXTHelper.NeuralNetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <Label>Learning rate</Label>
            <tk:DoubleUpDown Value="{Binding Path=LearningRate}"></tk:DoubleUpDown>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label>Iterations</Label>
            <tk:IntegerUpDown Value="{Binding Path=LearningRate}"></tk:IntegerUpDown>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label>Momentum</Label>
            <tk:IntegerUpDown Value="{Binding Path=Momentum}"></tk:IntegerUpDown>
        </StackPanel>

        <Button Command="{Binding RunLearningCommand}">Learn</Button>
    </StackPanel>
</UserControl>
