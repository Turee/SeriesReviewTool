<Window x:Class="TXTHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.codeplex.com"
        Title="Series review tool 0.1" Height="500" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="220"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="400"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <oxy:Plot Grid.Column="1" Title="Current series" Model="{Binding Path=CurrentPlotModel}"></oxy:Plot>
        <Grid Grid.Column="0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
            </Grid.RowDefinitions>
            <ListView Grid.Row="1" ItemsSource="{Binding Path=ExcelFiles,UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding Path=SelectedExcelFile}" SelectedIndex="{Binding Path=SelectedFileIndex,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=DisplayString}"></TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Path=SeriesToReviewText}"></TextBlock>
            <Button Command="{Binding Path=RefreshExcelDirectoryCommand}" Grid.Row="2">Refresh</Button>
        </Grid>
        <Grid Grid.Column="2" >
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"  Grid.Row="0" Text="{Binding Path=ReviewedSeriesText}"></TextBlock>
            <ListView Grid.Row="1" ItemsSource="{Binding Path=TextFiles,UpdateSourceTrigger=PropertyChanged}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Background="{Binding Path=Background}" Text="{Binding Path=DisplayString}"></TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Grid.Row="2" Command="{Binding Path=RefreshTextDirectoryCommand}">Refresh</Button>
            
        </Grid>
        
        <Grid Grid.Column="0" Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Path=InPath,UpdateSourceTrigger=PropertyChanged}"></TextBlock>
            <Button Grid.Row="1" Margin="5,5,5,5"  Command="{Binding Path=SelectExcelDirectoryCommand}">Select xlsx directory</Button>
        </Grid>

        <Grid Grid.Column="2" Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock TextWrapping="Wrap" Text="{Binding Path=OutPath,UpdateSourceTrigger=PropertyChanged}"></TextBlock>
            <Button Margin="5,5,5,5" Grid.Row="1" Command="{Binding Path=SelecteTextDirectoryCommand}">Select text directory</Button>
        </Grid>

        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Margin="5,5,5,5"  Grid.Column="1" Command="{Binding Path=AcceptSeriesCommand}" Content="Accept (F2)">
            </Button>
            <Button Margin="5,5,5,5"  Command="{Binding Path=DiscardSeriesCommand}" Content="Discard (F1)">
            </Button>
        </Grid>
    </Grid>


</Window>
